---
dependency:
  name: galaxy
driver:
  name: podman
#lint: |
  #set -e
  #yamllint .
  #ansible-lint
platforms:
  - name: instance-el8
    image: rockylinux:8
    privileged: true
    pre_build_image: true
  - name: instance-el9
    image: rockylinux:9
    privileged: true
    pre_build_image: true
provisioner:
  playbooks:
    prepare: prepare.yml
  name: ansible
verifier:

  name: ansible
