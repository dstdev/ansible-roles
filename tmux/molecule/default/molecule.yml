---
dependency:
  name: galaxy
driver:
  name: podman
platforms:
  - name: r8instance
    image: quay.io/rockylinux/rockylinux:8
    privileged: true
    command: "/usr/sbin/init"
    pre_build_image: true
provisioner:
  name: ansible
  playbooks:
    prepare: prepare.yml
verifier:
  name: ansible
